window.__FASTLINK_RUNTIME__ = Object.freeze({
  environment: "$VITE_FASTLINK_ENVIRONMENT",
  apiUrl: "$VITE_FASTLINK_API_URL",
  buildSha: "$RAILWAY_GIT_COMMIT_SHA",
  interactionMode: "$VITE_FASTLINK_INTERACTION_MODE"
})
